#include <stdio.h>
#include <conio.h>

main( )
{
      float base, altura, area;
      
      printf("Base: ");
      scanf("%f", &base);
      printf("Altura: ");
      scanf("%f", &altura);
      area=(base*altura)/2;
      printf("A area do triangulo é %f", area);
      
}
